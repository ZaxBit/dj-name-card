# Name Card Workshop

## Requirements

You need **Node.js v18 or higher**.

Check your version:
```
node -v
```

If the command is not found, or the version is below 18, install Node.js first:
→ https://nodejs.org (download the **LTS** version)

---

## Getting started

1. Open your terminal in this folder
2. Run: `npm run dev`
3. Open: http://localhost:5173
